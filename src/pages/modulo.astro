---
import "../styles/global.css";
import Header from "../components/Header.astro";
import pb from "../utils/pb";

/* ================= IMAGES POCKETBASE ================= */

const heroRecord = await pb.collection("image").getOne("3e4mhwufm8u6xy1");
const heroMockup = pb.files.getUrl(heroRecord, heroRecord.image);

const logoBlackRecord = await pb.collection("image").getOne("8ucz355bp416mzm");
const logoBlack = pb.files.getUrl(logoBlackRecord, logoBlackRecord.image);

const mockup1Record = await pb.collection("image").getOne("md0kwwpaoisjm1a");
const mockup1 = pb.files.getUrl(mockup1Record, mockup1Record.image);

const mockup2Record = await pb.collection("image").getOne("efob4gkzvt0zt84");
const mockup2 = pb.files.getUrl(mockup2Record, mockup2Record.image);

const mockup3Record = await pb.collection("image").getOne("0tpctffvmxy3fg8");
const mockup3 = pb.files.getUrl(mockup3Record, mockup3Record.image);

const moodboardRecord = await pb.collection("image").getOne("oaa44rxzxc4dlk7");
const moodboardImg = pb.files.getUrl(moodboardRecord, moodboardRecord.image);
---

<head>
  <!-- ✅ FIX GLOBAL DU PROBLÈME DE TEXTE -->
  <meta charset="utf-8" />
</head>

<Header />

<main class="bg-white text-black overflow-hidden">

  <!-- ================= HERO ================= -->
  <section class="max-w-7xl mx-auto px-6 sm:px-10 lg:px-16 pt-10 pb-20">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">

      <div>
        <p class="font-mono text-xs tracking-widest mb-4">PROJET</p>

        <h1 class="heading-font text-[42px] sm:text-[56px] lg:text-[72px] leading-none">
          MŌdulo
        </h1>

        <p class="font-mono text-sm max-w-md mt-6">
          Identité visuelle pour une marque éco-responsable spécialisée
          dans le mobilier modulaire.
        </p>
      </div>

      <div class="flex justify-end">
        <img
          src={heroMockup}
          alt="Logo Modulo"
          class="w-full max-w-[240px] sm:max-w-[260px] lg:max-w-[280px] object-contain"
        />
      </div>

    </div>
  </section>

  <!-- ================= CONCEPT ================= -->
  <section class="max-w-7xl mx-auto px-6 sm:px-10 lg:px-16 pb-24">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-16 items-start">

      <div class="lg:col-span-6">
        <h2 class="heading-font text-2xl mb-8">Concept</h2>

        <div class="font-mono text-sm leading-relaxed space-y-6 max-w-xl">
          <p>
            Le logo repose sur une construction géométrique composée de blocs
            modulaires. Cette structure traduit directement la notion de
            modularité, cœur de l’ADN de la marque.
          </p>

          <p>
            La palette associe un vert profond et un brun clair, évoquant la
            nature, la durabilité et les matériaux bruts.
          </p>
        </div>
      </div>

      <div class="lg:col-span-6">
        <div class="grid grid-cols-2 gap-12">
          <div class="text-center">
            <img src={heroMockup} class="w-[150px] mx-auto mb-3" />
            <p class="font-mono text-xs uppercase tracking-wide">Version couleur</p>
          </div>

          <div class="text-center">
            <img src={logoBlack} class="w-[150px] mx-auto mb-3" />
            <p class="font-mono text-xs uppercase tracking-wide">Version noir</p>
          </div>
        </div>
      </div>

    </div>
  </section>

  <!-- ================= DESIGN ================= -->
  <section class="bg-black text-white py-24">
    <div class="max-w-7xl mx-auto px-6 sm:px-10 lg:px-16">
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-16 items-center">

        <div class="lg:col-span-7 flex gap-8">
          <img src={mockup1} class="h-[210px] object-contain" />
          <img src={mockup2} class="h-[210px] object-contain" />
          <img src={mockup3} class="h-[210px] object-contain" />
        </div>

        <div class="lg:col-span-5">
          <h2 class="heading-font text-2xl mb-8">Design</h2>

          <div class="font-mono text-sm leading-relaxed space-y-6">
            <p>
              Le logo a été pensé pour être lisible et impactant sur tous
              les supports, du digital aux objets physiques.
            </p>

            <p>
              Sa structure modulaire garantit une grande flexibilité tout
              en conservant une identité forte et cohérente.
            </p>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ================= MOODBOARD ================= -->
  <section class="max-w-7xl mx-auto px-6 sm:px-10 lg:px-16 py-24">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-16 items-center">

      <div class="lg:col-span-5">
        <h2 class="heading-font text-2xl mb-8">Moodboard</h2>

        <div class="font-mono text-sm leading-relaxed space-y-6 max-w-md">
          <p>
            L’univers visuel s’inspire des matières naturelles, des formes
            simples et d’une palette douce et organique.
          </p>

          <p>
            Il traduit une vision durable, élégante et intemporelle du
            design d’intérieur.
          </p>
        </div>
      </div>

      <div class="lg:col-span-7">
        <div class="relative">
          <img src={moodboardImg} class="w-full max-w-[640px] ml-auto" />
          <div class="absolute -bottom-5 -left-5 w-full h-full border border-black"></div>
        </div>
      </div>

    </div>
  </section>

  <!-- ================= FOOTER ================= -->
  <footer class="bg-black w-full flex items-center justify-between text-white px-6 sm:px-10 lg:px-16 py-6">
    <div class="flex items-center gap-4">
      <a href="https://www.linkedin.com/in/alexandre-demling-965a90346" target="_blank" class="footer-link uppercase">LinkedIn</a>
      <a href="https://github.com/Alexandre-Demling" target="_blank" class="footer-link uppercase">Github</a>
    </div>
    <a href="/mentionlegale" class="footer-link uppercase">Mentions légales</a>
  </footer>

</main>

<style>
  .heading-font {
    font-family: var(--font-heading);
  }
</style>
